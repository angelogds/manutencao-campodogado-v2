<%- include("./permissions") %>

<aside class="sidebar">
  <div class="sidebar__brand">
    <div class="logo">Campo do Gado</div>
    <div class="role"><%= user ? user.role : "" %></div>
  </div>

  <nav class="sidebar__nav">
    <a href="/dashboard">Dashboard</a>

    <% if (hasAnyRole(["ADMIN","MANUTENCAO","RH"])) { %>
      <a href="/os">Ordens de Serviço</a>
    <% } %>

    <% if (hasAnyRole(["ADMIN","MANUTENCAO"])) { %>
      <a href="/solicitacoes">Solicitações</a>
    <% } %>

    <% if (hasAnyRole(["ADMIN","COMPRAS"])) { %>
      <a href="/compras">Compras</a>
      <a href="/estoque">Estoque</a>
    <% } %>

    <% if (hasAnyRole(["ADMIN","DIRECAO"])) { %>
      <a href="/dashboard/custos">Custos</a>
    <% } %>

    <% if (hasAnyRole(["ADMIN"])) { %>
      <a href="/admin/users">Usuários</a>
    <% } %>
  </nav>
</aside>
